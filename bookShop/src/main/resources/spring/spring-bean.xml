<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd">
    <context:component-scan base-package="com.qunar.qauction">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
    </context:component-scan>


    <!--swift account config-->
    <bean id="swiftClient" class="com.qunar.qauction.service.upload.impl.SwiftClient" init-method="initAccount">
        <property name="swiftName" value="${swift.username}"/>
        <property name="swiftPassWord" value="${swift.password}"/>
        <property name="authUrl" value="${swift.authUrl}"/>
    </bean>
    <bean id="swiftContainer" class="com.qunar.qauction.service.upload.impl.SwiftContainer" init-method="initContainer">
        <property name="name" value="${swift.img}"/>
    </bean>

    <!--   默认使用QSSO实现登录管理 -->
    <bean id="loginManager" class="qunar.web.security.DefaultLoginManager"/>
    <!--   bean 映射 -->
    <bean id="beanMapper" class="com.qunar.qauction.common.utils.beanmapper.OrikaBeanMapper">
        <property name="basePackage" value="com.qunar.qauction.model.**" />
    </bean>


    <!--email-->
    <!--线程池异步发送短信-->
    <bean id="taskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="15"/>
        <property name="maxPoolSize" value="50"/>
        <property name="queueCapacity" value="2000"/>
    </bean>
    <!--邮件发送实现类-->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host">
            <value>${mail.host}</value>
        </property>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.auth">25000</prop>
            </props>
        </property>
        <property name="username">
            <value>${mail.username}</value>
        </property>
        <property name="password">
            <value>${mail.password}</value>
        </property>
    </bean>
</beans>