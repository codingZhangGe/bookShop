<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xupt.bookshop.dao.ContactDao">

<insert id="insertContact" parameterType="com.xupt.bookshop.model.common.Contact">
    INSERT INTO contact (username, email, tel, message)
    VALUES (#{contact.username},#{contact.email},#{contact.tel},#{contact.message})
</insert>


</mapper>